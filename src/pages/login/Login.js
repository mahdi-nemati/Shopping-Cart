import LoginForm from "../login/LoginForm";
import Layout from "../../core/layout/Layout";

const Login = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default Login;
